/target
**/*.rs.bk
Cargo.lock.bak

# Nested git worktrees (e.g. .worktrees/<name>) — never part of the tree
/.worktrees/

# cargo-mutants output
/mutants.out/
/mutants.out.old/

# Large fuzzing seed corpus is kept on the dedicated fuzz-seed-corpus branch,
# not on main.
/fuzz/seed-corpus/
